# Supabase project used to store preorder signups.
# Get these from your Supabase project's Settings > API page.
# SUPABASE_SERVICE_ROLE_KEY is a secret — never expose it client-side,
# and add both as Environment Variables in the Vercel project settings.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
